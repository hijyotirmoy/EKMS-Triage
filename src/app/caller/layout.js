export const metadata = {
  title: "triage caller",
  description: "EKMS Triage Caller Portal",
  icons: {
    icon: "/logo.png",
    shortcut: "/logo.png",
    apple: "/logo.png",
  },
};

export default function CallerLayout({ children }) {
  return children;
}
